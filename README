A repository containing the presentation for the LUG meeting on programming competitions.

Requirements:

Pygments
Sphinx
rst2pdf

Usage:
The pdf should already be built in the top level directory. If not `make pdf` will build it from source, and place it in the top level directory
